#include<stdio.h>
int main() {
    double y = 365.2422;
    int d, h, m, s;

    d =(int) y;
    y = y - d;
    h =(int) (y * 24);
    y = y * 24 - h;
    m =(int) (y * 60);
    y = y * 60 - m;
    s =(int) (y * 60);
    printf("1년은 %d일, %d시간, %d분, %d초입니다.\n", d, h, m, s);

    return 0;
}
